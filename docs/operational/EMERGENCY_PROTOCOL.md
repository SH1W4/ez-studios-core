# 🚨 Emergency Protocol | Protocolo de Emergência

[**Português**](#%EF%B8%8F-portugu%C3%AAs-colapso-de-incerteza) | [**English**](#%EF%B8%8F-uncertainty-collapse)

---

## 🇺🇸 English: Uncertainty Collapse

### 1. Critical Failures
In procedural systems, a **Critical Failure** occurs when the **Algebraic Validation Engine** detects an unsolvable state (Entropy reaches zero with no valid tiles).

### 2. Immediate Recovery
- **Snapshot Rollback**: Revert the `intentCompiler` state to the last stable seed.
- **Heuristic Softening**: Automatically decrease constraint strictness by 10% to allow for a new collapse path.

### 3. Debugging the Neural Link
If the **Neural Assistant** exhibits "Glitch" behavior beyond 5 seconds:
- **Check Isomorphism**: Ensure local environment variables match the Cloud Vault secret keys.
- **Purge Cache**: Run `npm run docs:sync` to refresh the semantic memory.

---

## 🇧🇷 Português: Colapso de Incerteza

### 1. Falhas Críticas
Em sistemas procedurais, uma **Falha Crítica** ocorre quando o **Motor de Validação Algébrica** detecta um estado insolúvel (A Entropia chega a zero sem tiles válidos).

### 2. Recuperação Imediata
- **Rollback de Snapshot**: Reverte o estado do `intentCompiler` para a última semente (seed) estável.
- **Suavização de Heurísticas**: Diminui automaticamente o rigor das restrições em 10% para permitir um novo caminho de colapso.

### 3. Depurando o Elo Neural
Se o **Assistente Neural** exibir comportamento de "Glitch" por mais de 5 segundos:
- **Verificar Isomorfismo**: Garanta que as variáveis de ambiente locais correspondam às chaves secretas do Cloud Vault.
- **Limpeza de Cache**: Execute `npm run docs:sync` para atualizar a memória semântica.
